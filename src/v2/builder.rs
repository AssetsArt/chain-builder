//! Query builder (filled by later tasks). Minimal compiling stub for now.

/// Typed, dialect-aware query builder. Stub — implemented in a later task.
pub struct QueryBuilder<D>(core::marker::PhantomData<D>);
